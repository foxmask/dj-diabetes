[tox]
envlist = dj_diabetes
#skipsdist = True

[testenv]
setenv =
         DJANGO_SETTINGS_MODULE=dj_diabetes.settings
         PYTHONPATH={toxinidir}
         PYTHONHASHSEED=0

commands = python manage.py test -v2
